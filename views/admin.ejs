<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title> Daftar User</title>
  <style>
    body {
      background-color: #1e1e2f;
      color: #eee;
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      padding: 20px;
      margin: 0;
    }
    h1 {
      margin-bottom: 10px;
      color: #6a6aff;
      text-align: center;
    }
    h2 {
      margin-bottom: 15px;
      text-align: center;
      color: #a8a8ff;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      background: rgba(40, 40, 55, 0.85);
      padding: 20px;
      border-radius: 12px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.7);
    }
    ul {
      list-style-type: none;
      padding-left: 0;
    }
    li {
      background: #3a3a5c;
      margin-bottom: 10px;
      padding: 10px 15px;
      border-radius: 8px;
      font-size: 1.1rem;
      transition: background 0.3s;
    }
    li:hover {
      background: #5555aa;
    }
    .empty {
      text-align: center;
      font-style: italic;
      color: #aaa;
      margin-top: 30px;
    }
    a.button-back {
      display: inline-block;
      margin-top: 20px;
      text-decoration: none;
      background: #6a6aff;
      color: #fff;
      padding: 10px 25px;
      border-radius: 14px;
      font-weight: 600;
      box-shadow: 0 5px 15px rgba(106,106,255,0.6);
      transition: background 0.3s;
    }
    a.button-back:hover {
      background: #7b7bff;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Admin Panel</h1>
    <h2>Daftar User Terdaftar</h2>

    <% if (users.length === 0) { %>
      <p class="empty">Belum ada user terdaftar.</p>
    <% } else { %>
      <ul>
        <% users.forEach(user => { %>
          <li><strong>ID:</strong> <%= user.id %> — <strong>Username:</strong> <%= user.username %></li>
        <% }) %>
      </ul>
    <% } %>

    <a href="/" class="button-back">Kembali ke Halaman Login</a>
  </div>
</body>
</html>
